# Настройка переменных окружения в Easypanel

## 📝 Добавьте эти переменные в настройках проекта

1. Откройте панель Easypanel
2. Перейдите в проект `dvinyaninov`
3. Откройте раздел **"Environment Variables"** или **"Build Args"**
4. Добавьте следующие переменные:

```
YANDEX_WEBMASTER_TOKEN=y0__xCKiYykARjziz0gou2dkhZM3mLM3tpaEyBS9Tg5iBBT90VlOA
YANDEX_USER_ID=344130698
```

**Важно:** Переменные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` уже должны быть добавлены.

## ✅ После добавления

1. Сохраните изменения
2. Easypanel автоматически пересоберёт контейнер
3. Cron будет запускаться автоматически каждый день в 9:00

## 🔍 Проверка

После деплоя подключитесь к серверу и проверьте:

```bash
ssh root@88.218.121.213

# Найдите контейнер
docker ps | grep dvinyaninov

# Зайдите в контейнер (замените ID)
docker exec -it CONTAINER_ID sh

# Проверьте что cron запущен
ps | grep crond

# Проверьте cron задачу
cat /etc/crontabs/root

# Проверьте .env файл
cat /app/api/.env
```

## 📅 Расписание

Скрипт будет запускаться **каждый день в 9:00 утра** по времени сервера.

Логи сохраняются в `/var/log/seo-automation.log`

## 🔧 Изменение расписания

Если хотите изменить время запуска, отредактируйте `Dockerfile`:

```dockerfile
# Текущее: каждый день в 9:00
echo "0 9 * * * cd /app/api && node seo-automation.js >> /var/log/seo-automation.log 2>&1"

# Примеры:
# Каждый день в 6:00
echo "0 6 * * * cd /app/api && node seo-automation.js >> /var/log/seo-automation.log 2>&1"

# Каждые 12 часов
echo "0 */12 * * * cd /app/api && node seo-automation.js >> /var/log/seo-automation.log 2>&1"

# Каждый понедельник в 9:00
echo "0 9 * * 1 cd /app/api && node seo-automation.js >> /var/log/seo-automation.log 2>&1"
```

## ⚠️ Важно

После добавления переменных окружения в Easypanel **пересоздайте токен** для безопасности (он был опубликован в чате).
